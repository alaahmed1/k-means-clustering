# k-means-clustering
K-Means clustering algorithm implemented from scratch in Python , where no ML libraries used. Built as a university assignment for the Data Visualization and Analysis course at GUtech. Supports manual or random 2D point input, visualizes each iteration live with matplotlib, and converges automatically using Euclidean distance.
